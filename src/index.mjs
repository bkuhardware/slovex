import Slovex from '../dist/slovex.common.js'

const {
    createStore,
    install,
    mapStateToComputed,
    mapDispatchToMethods
} = Slovex;

export {
    Slovex as default,
    createStore,
    install,
    mapStateToComputed,
    mapDispatchToMethods
}
